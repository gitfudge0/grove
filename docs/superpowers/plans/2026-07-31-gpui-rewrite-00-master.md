# gpui Rewrite — Master Plan Index

> **For agentic workers:** This is an index, not an executable plan. Execute the numbered phase plans in order; each is a standalone plan document following superpowers:writing-plans. Later plans are written just-in-time when their phase starts, incorporating findings from earlier phases.

**Spec:** `docs/superpowers/specs/2026-07-31-gpui-rewrite-design.md` (the authority for all behavior; Appendix A is the acceptance checklist)

**Branch:** all work happens on `gpui-rewrite` (big-bang branch off `main`).

| # | Plan | Exit gate | Status |
|---|------|-----------|--------|
| 01 | Spikes (terminal element, text inputs, zoom, Linux matrix) | Findings doc committed; go/no-go + rev pins decided | done — findings committed; awaiting user go/no-go |
| 02 | `crates/grove-terminal` + dual-parser golden harness | Golden tests green against vt100 oracle | done — 8/8 golden tests green on rustc 1.94.1 (default toolchain, no `rust-toolchain.toml` needed). Two asserted divergences amend spec §3: (a) the **primary screen reflows on resize** — `Term::resize` hardcodes `self.grid.resize(!is_alt, ..)`, no config knob, so "reflow-on-resize is suppressed" is unachievable without patching alacritty; the alt screen (tmux, Grove's actual regime) agrees with vt100. (b) alacritty retains an `ED 2`-cleared screen in scrollback where vt100 drops it. Both pinned by `crates/grove-terminal/tests/divergence.rs`. |
| 03 | App shell (entities/globals, theme, fonts, zoom, keymap skeleton, AnimationClock, storage) | Shell opens, themed, metric assertion passes | done — startup metric assertion passes at **cell_w = 7.5000005** (`GROVE_GPUI_SELFTEST=1`, within the 0.001 epsilon of 7.5); 39 unit tests + clippy clean on rustc 1.95.0. **Toolchain split:** `crates/grove-gpui` is a workspace member but excluded from `default-members`, so bare `cargo build`/`cargo test` stays on 1.94.1 for the iced app and the core crates; grove-gpui is built explicitly with `cargo +1.95.0 -p grove-gpui`. CI's four `--workspace` invocations were rewritten to `-p grove -p grove-core -p grove-terminal` (grove-gpui is CI-excluded until Plan 10). **The gpui-component durable-pin decision is deferred to Plan 08** — this phase has no text inputs, so no `gpui-component` dependency and no `[patch]` section exist in the main workspace. **Carry-forward for Plan 10:** grove-gpui's clippy runs with `--no-deps`, because clippy 1.95 raises 9 new lints (`map_unwrap_or`, `duration_suboptimal_units`, …) in `crates/grove-core`, which is off-limits this phase — they must be cleaned up when the whole product moves to 1.95. |
| 04 | TerminalElement + single-session workspace + full input path | Spec §Terminal checklist rows green; keyboard byte-table test green | done (pending human sign-off on the Appendix A **Terminal** rows, which are listed in plan 04 Task 6 Step 3) — **keyboard byte-table test green** (17 cases in `terminal::keys`, incl. the parity rows pinning plain `\x1b[A..D` for modified arrows and DECCKM affecting `arrow_moves` only); 105 unit tests + clippy clean on rustc 1.95.0, iced app and grove-core untouched and green on 1.94.1. **Inverse swap:** `GroveTerm::snapshot()` does **not** pre-apply it — `Cell` carries `inverse: bool` raw (`cell.rs:20`), so `terminal::colors::resolve_pair` owns the pipeline's single swap. **CJK `force_width`:** exists at the pinned rev (`gpui/src/text_system.rs:397`) and is wired behind `forced_width`, driven by a UAX #11 wide-range table; per-run anchoring remains the primary mitigation. Appendix A row 2 decides whether the helper survives. **Pinch-zoom:** the pinned rev's `ScrollDelta` has only `Pixels`/`Lines` and exposes **no distinguishable pinch/magnify event**, so zoom is modifier+wheel only. **Amendment to Constraint 3:** `crates/grove-terminal/src/pty.rs` gained one method, `PtyHandle::take_receiver()`, because the plan's zero-wakeup blocking reader is not expressible while the handle only lends a `!Sync` `&Receiver`; nothing else in grove-terminal changed and its golden suite stays green. `arboard` and `base64` were hoisted to `[workspace.dependencies]` (same versions/features) so both front ends share one copy. |
| 05 | Sidebar tree + WorkspaceState sync | Sidebar checklist rows green | done (pending human sign-off on the Appendix A **sidebar** rows, listed in plan 05 Task 7 Step 2) — 182 unit tests + clippy clean on rustc 1.95.0; iced app, grove-core and grove-terminal untouched and green on 1.94.1. **`uniform_list` vs plain column (carried amendment 2): plain scrollable column.** `uniform_list` at ZED_REV `1a246ef` does not merely prefer uniform heights, it measures one element and lays every other row out at that height (`gpui/src/elements/uniform_list.rs:1-5`, single `item_to_measure_index`) — there is no per-row height hook, and Grove's worktree rows are 28 or 42px (`src/gui/rows.rs:268`). `views::rows::row_height` stays the single height source for the column, `TreeRow::height` and `agent_menu_top`; `gpui::list` (variable heights) is the upgrade path if row counts ever justify virtualization. **`Svg` tint at this rev is `text_color`, not `color`** — `Svg` has no `color` method and paints from `style.text.color` (`gpui/src/elements/svg.rs:110`). **No grove-core amendment was needed:** `git::{is_repo, list_worktrees, worktree_git_state, git_state_suffix}` and `Store::active_projects` covered the phase; `path_basename` was reimplemented in grove-gpui with tests per Constraint 3 candidate 1. **Deviations:** no `IndexMap` ("tech stack additions: none" outranks the sketch — insertion order is a `Vec<SessionMeta>` beside a `HashMap` of entities, which also makes every registry rule unit-testable without a PTY); `$HOME` instead of the `dirs` crate; `clamp_sidebar_width` landed in Task 1 (its consumer) rather than Task 5; `SessionId`/`SessionMeta` landed in Task 1 because Task 1's tests need the key type; the iced per-frame PTY-theme memo is deliberately **not** ported (the resolution is a `Store` read done fresh in `prepaint`, so the toggle re-colors on the next frame with no bookkeeping — recorded in a comment at the call site). **Stub-gated:** every session classifies `Idle` until Plan 06's 480ms classifier, so `JumpToWaitingSession` is a no-op today and the roll-up/spinner/amber-pulse glyphs render but never change state. |
| 06 | Appbar/statusbar/attention/dock/toasts + 480ms activity task | Attention checklist rows green on both platforms | done (pending human sign-off on the Appendix A **Attention/activity** rows — the checklist is `docs/superpowers/plans/2026-07-31-gpui-rewrite-06-attention-checklist.md`, to be run on Wayland **and** X11) — 234 unit tests + clippy clean on rustc 1.95.0; iced app, grove-core and grove-terminal untouched and green on 1.94.1. **No grove-core amendment was needed** — `attention::{prepare, read_state, acknowledge, cleanup, cleanup_stale_files, STATE_FILE_ENV}`, `claude_agents::Poller` and `tmux::{new_session, pane_pid}` covered the whole pipeline as-is. **Classifier port:** all 33 classifier tests from `src/gui/activity.rs` came across unedited, RED-then-green against a stub `classify` (the plan's "51 unit tests" is a miscount — the oracle has 37 `#[test]` fns, 4 of them the `most_urgent` roll-ups Plan 05 already ported as 3). **gpui window-activation API used:** `Context::observe_window_activation(&mut Window, …)` + `Window::is_window_active()` (verified at ZED_REV, `gpui/src/app/context.rs:444`, `window.rs:2352`), registered on the first `render` because `Workspace::new` has no `Window`. **`cached_context` memo: omitted**, recorded at the call site in `views/sidebar.rs::row_ctx` — the iced memo exists because the sanitize ran per frame per row inside `view()`; revisit only with a profile. **`spawn_native` finding (Task 1 Step 1): the native fallback spawns a bare login shell and never invokes the agent** — `Agent::invocation()` plumbing (`session.rs:248-252`) is absent, which reads as a deliberate Plan 05 deferral (its doc comment says "in spirit"). Only the attention plumbing was fixed there (`GROVE_STATE_FILE` on the shell env); **this gap still needs an owner** (orchestrator: likely Plan 09/10 close-out). Consequence: `TerminalSession::root_pid()` is `None` for `Backend::Native`, because `PtyHandle` exposes no pid accessor and grove-terminal is read-only — harmless, since native never runs an agent and the poller is Claude-only. **Deviations:** (1) the amber pulse is **`with_animation`-free** — spec §4 maps it to `with_animation`, but that animates an element being rendered while this pulse is a scalar read by six call sites across two views, so `activity_store::pulse_at` is a tested `EaseInOut` triangle wave over `Instant` (1000ms half-period, auto-reverse, constant `0.0` while nothing waits); (2) `WorkspaceState::acknowledge` records the id and `ActivityStore::acknowledge` applies **both** halves (tracker + file truncate) via an observer that drains in the same frame — `WorkspaceState` owns no entity handles and its transitions are pure, and doing it inline would have meant updating an entity from inside its own update; (3) the waiting queue's tree order comes from a `visible_order` cache the rail publishes on `WorkspaceState`, since `visible_session_order()` lives on `Sidebar`, not `WorkspaceState`; (4) appbar/statusbar/session-header are **free render functions**, not `Render` entities — they own no state, exactly like `views::rows`, and clicks travel out through a `ChromeAction` dispatch; (5) `#![forbid(unsafe_code)]` became `#![deny(...)]` so `platform/dock.rs` can carry one audited macOS `allow`; (6) `attention::prepare` is skipped under `cfg!(test)` so the registry's pure tests do not litter the user's config dir. **Stub-gated / deferred:** the `+`, the grid toggle, the cog and both statusbar chips dispatch to logged Plan 07/08 stubs; the cog's upgrade dot is stubbed `false` (Plan 09); `animation_clock::toast_pulse` stays deliberately unconsumed (it belongs to Plan 07's tile scrim, not to the non-pulsing statusbar toast); `ToastState` ships tested but has **no producer yet** — every one is a Plan 07/08 modal or action, so checklist row 16 is not verifiable without a temporary stub call; the macOS dock badge/bounce ships here but is MANUAL-on-macOS, deferred to Plan 10. |
| 07 | Grid/zen/terminal tab/worktree panel | Grid+zen checklist rows green | done (pending human sign-off on the Appendix A **grid/zen/terminal-tab/panel** rows — the checklist is Task 7 Step 2 of `docs/superpowers/plans/2026-07-31-gpui-rewrite-07-grid-zen.md`, rows 1-13, to be run side by side against `~/.local/bin/grove`) — 294 unit tests + clippy clean on rustc 1.95.0 (was 234); iced app, grove-core and grove-terminal untouched and green on 1.94.1. **No grove-core amendment was needed** — `Store::grid_order`, `storage::persist`, `Agent` and the Plan 05/06 spawn surface covered the phase as-is. **Amendment 2's two parity assertions: exact, delta 0.** At 1280x800 / zoom 1.0 the gpui tile lands on the iced oracle's `(rows, cols)` for every case measured — n=2/k=1 (40,81), n=3/k=2 (18,81), n=3/k=1 (40,81), n=5/k=2 (18,52), n=5/k=1 (40,52) — and the panel split matches at 20%/40%/75% (21/46/90 cols). Both assertions live in the test modules of `views/grid.rs` and `views/term_panel.rs` with the oracle formula reimplemented locally, never exported. **`screen_from_flags` needed no `Screen::Terminal` answer (Task 3 Step 3): there is no such variant.** iced's own enum has exactly three (`src/gui/update/shortcuts.rs:87-91`); the terminal tab is `terminal_focused`, orthogonal to the screen model, so the ported `screen_from_flags`/`contexts_for` already agree and no fifth state was invented (pinned by `keymap::tests::the_screen_model_has_exactly_three_states`). **The panel is suppressed in grid view (Task 6 Step 4): yes, with no exception.** `workspace()` returns `grid_workspace()` at `src/gui/view/terminal.rs:182-184`, *before* the split at `:204`; `focused_session` (`pty_input.rs:164-166`) and `selection_pane` (`terminal.rs:1192-1198`) both check `grid_view` first. **The keymap's grid carve-outs are deleted**: `only_the_grid_actions_remain_unbound` is replaced by the strictly stronger `no_registry_action_is_left_unbound`, and the `matches!(sc, GridMove | GridSwap) { continue }` skip is gone — both drift guards now cover every row. **Plan-text correction:** Task 1 Step 1's n=3 example ("from tile 1, a left move must land on tile 2") contradicts iced's own test, which asserts `grid_neighbor(1, 3, -1, 0) == Some(0)` (`shortcuts.rs:1248`); the downward row clamp shows up on the mirror move, `grid_neighbor(2, 3, 1, 0) == Some(1)`. `grid_neighbor` was ported verbatim and iced's two test blocks came across unedited. **Deviations:** (1) `tile_order` is `Vec<SessionId>`, so iced's index-shifting hazard is gone, but `reconcile_tile_order` still matches `Store::grid_order`'s string keys and returns positions; (2) `persist_grid_order` is staged as `Option<Vec<SessionId>>` on `WorkspaceState`, not a dirty bit, because `exit_grid` persists *before* clearing `tile_order`; (3) `pty_selection = None` is not ported into the transitions — each `TerminalView` owns its own selection; (4) the tile slide is CSS-relative `inset` (an offset that moves drawing, not layout), replacing iced's custom `slide.rs` widget, and `EaseOutCubic` is written out arithmetically since `iced::animation::Easing` is not a dependency; (5) grid tiles and panel shells pad their PTY 16/12 exactly as iced's `pty()` does — which is what makes the parity assertions exact — while the **single-session body stays unpadded from Plan 04**, a pre-existing choice this phase did not re-open (orchestrator: worth a look during the Plan 10 screenshot sweep); (6) `GridAction` has no `Release` variant — the drag's release edge is listened for at the root, since a pointer released outside its tile must still commit; (7) `TerminalSession::spawn` never returned a `Result`, so it now records a `spawn_error` string that the two toast producers read. **First toast producers wired (recorded ambiguity 5):** `"terminal failed: {e}"` on a failed home-terminal restart and on a failed panel-shell spawn (`src/app/terminals.rs:104-108`). The agent-session producer (`"failed to start session: {e}"`, `sessions.rs:482`) is **not** wired: agent spawns live in `Sidebar::spawn_session`, which this phase did not touch — carry it into Plan 08. Checklist row 13 is therefore verifiable by making a *shell* unspawnable. **Stub-gated / deferred:** the `+` segment, the cog, both statusbar chips, `NewSession`/`NewSessionInWorktree`/`SwitchSession`/`Settings`/`ShortcutOverlay` and the session bar's `run script` all still dispatch to logged Plan 08 stubs; tmux sidecar reattach and the upgrade dot stay Plan 09; the scripted screenshot sweep stays Plan 10. |
| 08 | Modals (simple → text-heavy) | All modal checklist rows green; keyboard matrix green | pending |
| 09 | Upgrade flow, telemetry, quit paths, persistence | System checklist rows green | pending |
| 10 | Parity passes (screenshot sweep, idle-power) + delete vt100/iced | Full Appendix A signed off; iced gone; `./install.sh` green | pending |

**Standing rules for every phase plan:**
- gpui + gpui-component pinned to the git revs decided in Plan 01; never bump mid-phase.
- vt100 stays in-tree as the golden-test oracle until Plan 10.
- Behavior questions are answered by reading the iced code (file refs in spec Appendix A), never by guessing.
- rustfmt only touched files with `--edition 2021`; clippy `unwrap_used`/`expect_used` deny applies.
- Run `./install.sh` at the end of each phase.
- Toolchain: rustc 1.95.0 via user-local rustup (`spikes/rust-toolchain.toml`); gpui_platform bootstrap + [patch] pin per findings doc.
